# GitWhy Homebrew Tap

Homebrew formula for [GitWhy](https://gitwhy.dev) — the context layer for git.

## Install

```bash
brew install gitwhy-cli/tap/git-why
```

## Usage

```bash
git why setup      # Set up GitWhy in your repo
git why --version  # Verify installation
```
